"""
flux2_klein: JAX-native inference for FLUX.2 Klein-4B.

This package expects the notebook (or any other caller) to import
specific functions rather than reimplement logic inline: the notebook
is a thin caller, everything that runs lives here.
"""
